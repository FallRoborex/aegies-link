[package]
name = "aegis-link"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
uuid = { version = "1.0", features = ["v4", "v7"] }
# ratatui = "0.29"  → Phase 2 dashboard